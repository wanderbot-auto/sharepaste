# desktop-linux

Planned Linux desktop client shell.

Suggested direction:

- Keep desktop-environment integration and packaging here.
- Share protocol and sync logic through `packages/client-core`.
- Isolate distro-specific packaging from the shared runtime layer.
