# client-core

Reserved extraction target for shared client runtime logic.

Expected future contents:

- crypto and protocol orchestration
- sync engine and history handling
- state model and persistence contracts
- platform adapter interfaces for clipboard, filesystem, and discovery

Current implementation still lives in `apps/client-cli/src`.
