{
  "name": "sharepaste",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "build": "npm run -w server build && npm run -w client build",
    "test": "npm run -w server test && npm run -w client test",
    "lint": "npm run -w server lint && npm run -w client lint",
    "desktop:macos:dev": "swift run --package-path macos sharepaste-desktop",
    "desktop:macos:build": "swift build --package-path macos -c release"
  }
}
